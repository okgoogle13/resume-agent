# Production Environment Variables for Google Cloud Run
# Copy this to set environment variables in Cloud Run

# Environment
ENVIRONMENT=production
DEBUG=false

# API Keys (Set these in Cloud Run environment variables)
# OPENAI_API_KEY=your_openai_api_key_here
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Database (Use Cloud SQL or Firebase in production)
DATABASE_URL=sqlite:///./resume_agent.db

# Security (Generate a strong secret key)
# SECRET_KEY=your_very_strong_secret_key_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings (Update with your frontend domain)
CORS_ORIGINS=["https://your-frontend-domain.com"]

# Server settings
PORT=8000
HOST=0.0.0.0
